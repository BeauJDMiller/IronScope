{
    "name": "ironscope-monorepo",
    "private": true,
    "workspaces": ["client", "server"],
    "scripts": {
        "start": "node server/server.js",
        "build": "cd client && npm install && npm run build"
    }
  }
  